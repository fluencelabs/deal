query DealQuery(
  $id: ID!
) {
  deal(id: $id) {
    id
    maxWorkersPerProvider
    pricePerWorkerEpoch
    paymentToken {
      id
    }
    targetWorkers
    addedComputeUnits {
      id
      provider {
        id
      }
    }
    effectors {
      effector {
        id
      }
    }
  }
}
